{
  "name": "simpliflow",
  "homepage": ".",
  "scripts": {
    "dev": "vite",
    "build": "vite build", 
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist --dotfiles"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}